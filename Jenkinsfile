pipeline {
    agent any
    stages {
      stage('Install dependencies'){
        steps {
          echo ("start install")
          sh("npm install")
          echo ("end install")
        }
      }
    }
}